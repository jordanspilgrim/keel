# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# Local datastore (regenerated by synth.py / run_demo.py)
keel.db
keel.db-wal
keel.db-shm

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Dashboard export artifact (regenerated by dashboard/export.py)
dashboard/data.json

# OS
.DS_Store

# Claude Code harness (roster + commands resolve globally, not from this repo)
.claude/.dlb-role
.claude/worktrees/
.claude/settings.local.json
