# Copy to .env and fill in. .env is gitignored — never commit a real key.
OPENAI_API_KEY=sk-...

# Optional overrides (defaults live in config.py):
# KEEL_FLAGSHIP_MODEL=gpt-5
# KEEL_MINI_MODEL=gpt-5-mini
# KEEL_EMBEDDING_MODEL=text-embedding-3-small
